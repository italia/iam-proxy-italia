# Security-pinned versions (Django 4.2 LTS, djangosaml2 1.11.x).
# Run: pip-audit -r requirements.txt
Django>=4.2.17,<5.0
cffi>=1.17.0
# CVE-2026-26007: SECT curves subgroup validation; fixed in 46.0.5
cryptography>=46.0.5
# pyOpenSSL 25.x required for cryptography 46.x; supports cryptography>=45.0.7,<47
pyOpenSSL>=25.0.0,<26.0.0

# django saml2 SP (requires Django>=4.2, defusedxml, pysaml2)
djangosaml2>=1.11.1
importlib-resources>=5.12.0,<6.0.0

# Required by Python zoneinfo when system tzdata is missing (Docker/slim images)
tzdata>=2024.1

# design-django-theme (bootstrap_italia_template)
design-django-theme>=2.7.0
django-sass-processor>=1.2.2
