apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iam-proxy-italia.fullname" . }}-test-connection"
  labels:
    {{- include "iam-proxy-italia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iam-proxy-italia.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
