==========================================
IAM Proxy Italia has been installed!
==========================================

{{- if and .Values.secret.create (not .Values.secret.existingSecret) }}
{{- $secretName := include "iam-proxy-italia.secret" . }}
{{- $existingSecret := lookup "v1" "Secret" .Release.Namespace $secretName }}

{{- if $existingSecret }}
✅ Using existing secret: {{ $secretName }}
   (Secrets preserved from previous installation)
{{- else }}
{{- $hasAutoGenerated := false }}
{{- if not .Values.satosa.encryption.key }}{{ $hasAutoGenerated = true }}{{- end }}
{{- if not .Values.satosa.encryption.salt }}{{ $hasAutoGenerated = true }}{{- end }}
{{- if not .Values.satosa.encryption.stateKey }}{{ $hasAutoGenerated = true }}{{- end }}
{{- if not .Values.satosa.encryption.userIdHashSalt }}{{ $hasAutoGenerated = true }}{{- end }}
{{- if not .Values.mongodb.password }}{{ $hasAutoGenerated = true }}{{- end }}
{{- if not .Values.mongodbBackend.password }}{{ $hasAutoGenerated = true }}{{- end }}
{{- if not .Values.mongodbFrontend.password }}{{ $hasAutoGenerated = true }}{{- end }}

{{- if $hasAutoGenerated }}
🔐 Auto-generated secure random secrets!
   The following values were automatically generated:
{{- if not .Values.satosa.encryption.key }}
   - satosa.encryption.key (32 chars)
{{- end }}
{{- if not .Values.satosa.encryption.salt }}
   - satosa.encryption.salt (16 chars)
{{- end }}
{{- if not .Values.satosa.encryption.stateKey }}
   - satosa.encryption.stateKey (32 chars)
{{- end }}
{{- if not .Values.satosa.encryption.userIdHashSalt }}
   - satosa.encryption.userIdHashSalt (16 chars)
{{- end }}
{{- if not .Values.mongodb.password }}
   - mongodb.password (24 chars)
{{- end }}
{{- if not .Values.mongodbBackend.password }}
   - mongodbBackend.password (24 chars)
{{- end }}
{{- if not .Values.mongodbFrontend.password }}
   - mongodbFrontend.password (24 chars)
{{- end }}

⚠️  IMPORTANT: These values are stored in the secret and will persist across upgrades.
   To retrieve them: kubectl get secret {{ $secretName }} -o yaml
{{- else }}
✅ Using custom secret values from values.yaml
{{- end }}
{{- end }}

Secret name: {{ $secretName }}

For production, consider using an external secret management system.
See examples/existing-secret.yaml for details.
{{- else if .Values.secret.existingSecret }}

✅ Using existing secret: {{ .Values.secret.existingSecret }}
{{- end }}

Configuration:
  Hostname: {{ .Values.satosa.hostname }}
  Debug mode: {{ .Values.satosa.debug }}
  MongoDB: {{ .Values.mongodb.host }}:{{ .Values.mongodb.port }}

==========================================

1. Get the application URL by running these commands:
{{- if .Values.httpRoute.enabled }}
{{- if .Values.httpRoute.hostnames }}
    export APP_HOSTNAME={{ .Values.httpRoute.hostnames | first }}
{{- else }}
    export APP_HOSTNAME=$(kubectl get --namespace {{(first .Values.httpRoute.parentRefs).namespace | default .Release.Namespace }} gateway/{{ (first .Values.httpRoute.parentRefs).name }} -o jsonpath="{.spec.listeners[0].hostname}")
  {{- end }}
{{- if and .Values.httpRoute.rules (first .Values.httpRoute.rules).matches (first (first .Values.httpRoute.rules).matches).path.value }}
    echo "Visit http://$APP_HOSTNAME{{ (first (first .Values.httpRoute.rules).matches).path.value }} to use your application"

    NOTE: Your HTTPRoute depends on the listener configuration of your gateway and your HTTPRoute rules.
    The rules can be set for path, method, header and query parameters.
    You can check the gateway configuration with 'kubectl get --namespace {{(first .Values.httpRoute.parentRefs).namespace | default .Release.Namespace }} gateway/{{ (first .Values.httpRoute.parentRefs).name }} -o yaml'
{{- end }}
{{- else if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "iam-proxy-italia.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
     NOTE: It may take a few minutes for the LoadBalancer IP to be available.
           You can watch its status by running 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "iam-proxy-italia.fullname" . }}'
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "iam-proxy-italia.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo http://$SERVICE_IP:{{ .Values.service.port }}
{{- else if contains "ClusterIP" .Values.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "iam-proxy-italia.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visit http://127.0.0.1:8080 to use your application"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:$CONTAINER_PORT
{{- end }}
