# Preferred language from Accept-Language header (en/it, default: en).
# Used by sub_filter in error_page.html and disco.html locations.
map $http_accept_language $preferred_lang {
    default en;
    ~*^en     en;
    ~*^it     it;
}
